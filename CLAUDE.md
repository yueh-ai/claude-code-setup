# Claude Code Project Guidelines

## Environment

This project runs in a **dev container** (VS Code Dev Containers / GitHub Codespaces). See `.devcontainer/` for setup details.

## Network Firewall

The dev container runs a restrictive firewall. See `.devcontainer/init-firewall.sh` for allowed domains.
