# Claude Code Project Guidelines

## Environment

This project runs in a **dev container** (VS Code Dev Containers / GitHub Codespaces) on a **remote host machine**. See `.devcontainer/` for setup details.

## Network Firewall

The dev container runs a restrictive firewall on the remote host. See `.devcontainer/init-firewall.sh` for allowed domains.
